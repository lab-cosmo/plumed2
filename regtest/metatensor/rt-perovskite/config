plumed_modules=metatensor
plumed_needs=metatensor
type=driver
# this is to test a different name
arg="--plumed plumed.dat --ixyz config.xyz --dump-forces forces --dump-forces-fmt %8.4f --length-units A" # --debug-forces forces.num"

function plumed_regtest_before(){
  python soap_cv.py
  echo Generated model using soap_cv.py
}
